#!/bin/sh

folder="$HOME/Imagens/Screenshots/"
cd "$folder" || exit

alias scrot='scrot -q 100'

case $1 in
      fullscreen )
	    scrot ;;
      select )
	    scrot -s ;;
      current )
	    scrot -u ;;
      * )
	    scrot ;;
esac
